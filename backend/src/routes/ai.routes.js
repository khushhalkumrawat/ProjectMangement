const express = require('express');
const router = express.Router();

// Placeholder - will be implemented
router.get('/', (req, res) => {
  res.json({ status: 'success', message: 'AI routes - Coming soon' });
});

module.exports = router;
